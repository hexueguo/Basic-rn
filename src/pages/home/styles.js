import { createStyles } from "../../utils/index";

const styles = {
  root: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  title: {
    color: "#000",
    fontWeight: "500",
    fontSize: 30,
  },
};

export default createStyles(styles);
